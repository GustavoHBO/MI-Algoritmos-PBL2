#ifndef FUNCOES_H_INCLUDED
#define FUNCOES_H_INCLUDED

/*
*   Função responsável por exibir o menu ao usuário.
*/

    int menu(void);


/*
*   Função que busca o arquivo matriz.txt, se existir retorna 0 se não existir retorna 1.
*/
    int buscar_matriz(void);


/*
*   Função que busca o arquivo de entrada, se existir retorna 0 se não retorna 1.
*/

    int buscar_entrada(void);


/*
*   Função que gera matriz com base nas medalhas adicionadas.
*/

    void gerar_matriz(int [][5], int);


/*
*   Função responsável por adicionar medalhas ao arquivo.txt.
*/

    void adicionar_medalhas(int, int*);

/*
*   Função responsável por ler e gerar a matriz.
*/

    void ler_entrada(int matriz[][5]);


/*
*   Função responsável por ler o aquivo de matriz;
*/

    void ler_matriz(int matriz[][5]);

/*
*   Função responsável por gerar o ranking.
*/

    int gerar_ranking(int *);

/*
*   Função responsável por ordenar a matriz pela quantidade total de medalhas na posição 3 da matriz.
*/

    void ordenar_total(int matriz[][5], int);

/*
*   Função responsável por ordenar as medalhas por ouro / prata / bronze, (posição 0, 1, 3, respectivamente).
*/

    void ordenar_medalhas(int matriz[][5], int paises);

#endif // FUNCOES_H_INCLUDED
